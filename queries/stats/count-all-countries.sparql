PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?location (COUNT(DISTINCT ?sample) AS ?sampleCount)
WHERE {
  ?sample prov:atLocation ?location .
}
GROUP BY ?location
ORDER BY DESC(?sampleCount)


