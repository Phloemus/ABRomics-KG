PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT ?aroType (COUNT(DISTINCT ?instance) AS ?instanceCount)
WHERE {
  ?instance rdf:type ?aroType .
  FILTER(STRSTARTS(STR(?aroType), "http://purl.obolibrary.org/obo/ARO_"))
}
GROUP BY ?aroType
ORDER BY DESC(?instanceCount)
