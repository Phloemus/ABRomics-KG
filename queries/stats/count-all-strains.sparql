PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX aro: <http://purl.obolibrary.org/obo/ARO_>

SELECT (COUNT(DISTINCT ?instance) AS ?aroStrainCount)
WHERE {
  ?instance rdf:type aro:Strain .
}
